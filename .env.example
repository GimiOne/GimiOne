BOT_TOKEN=123456:ABCDEF_YOUR_TOKEN

# TG IDs с полным доступом (через запятую). Для них бот создаёт подписку без оплаты.
ADMIN_TG_IDS=372512028

# SQLite
SQLITE_PATH=./db/bot.sqlite3

# X-UI / 3X-UI (panel must be доступен только локально)
XUI_BASE_URL=http://127.0.0.1:54321
XUI_USERNAME=admin
XUI_PASSWORD=change_me

# Если не задан, бот попытается найти inbound по remark
XUI_INBOUND_ID=
XUI_INBOUND_REMARK=vless-reality

# Публичный хост (домен/IPv4) для VLESS-ссылки
VPN_PUBLIC_HOST=vpn.example.com

# Подписка
SUBSCRIPTION_DAYS=30
SUBSCRIPTION_PRICE_RUB=199

# Фоновая проверка истечения подписок
SUBSCRIPTION_WATCH_INTERVAL_SEC=60
